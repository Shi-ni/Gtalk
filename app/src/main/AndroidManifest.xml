<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chata">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/gtalk_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/gtalk_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GroupActivity"></activity>
        <activity
            android:name=".ForgetPasswordActivity"
            android:parentActivityName=".LoginActivity" />
        <activity
            android:name=".find_firends"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".groupDepartment" />
        <activity
            android:name=".profile"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".group_chat"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>